apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opcua.fullname" . }}-test-connection"
  labels:
    {{- include "opcua.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opcua.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
